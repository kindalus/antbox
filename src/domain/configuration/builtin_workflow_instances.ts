import type { WorkflowInstanceData } from "./workflow_instance_data.ts";

/**
 * Builtin workflow instances
 * Workflow instances are runtime data - there are no builtin instances
 */

export const BUILTIN_WORKFLOW_INSTANCES: readonly WorkflowInstanceData[] = [];
