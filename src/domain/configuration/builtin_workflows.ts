import type { WorkflowData } from "./workflow_data.ts";

/**
 * Builtin workflows
 * Currently, there are no builtin workflows in the system
 */

export const BUILTIN_WORKFLOWS: readonly WorkflowData[] = [];
