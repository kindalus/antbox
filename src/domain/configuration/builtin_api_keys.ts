import type { ApiKeyData } from "./api_key_data.ts";

/**
 * Builtin API keys
 * Currently, there are no builtin API keys in the system
 */

export const BUILTIN_API_KEYS: readonly ApiKeyData[] = [];
